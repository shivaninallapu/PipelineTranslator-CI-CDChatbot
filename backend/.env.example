# .env.example
LANGCHAIN_SERVICE_URL=http://localhost:8001   # Person 4's service (adjust if same process)
LLM_PROVIDER=localai                        # ollama | openai | localai
OLLAMA_URL=http://localhost:11434                    # only if using OpenAI
LOCALAI_URL=http://localhost:8080
FRONTEND_URL=http://localhost:5173            # for CORS
UPLOAD_DIR=./uploads                          # temp file storage
MAX_FILE_SIZE_MB=5